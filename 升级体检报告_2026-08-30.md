# 升级体检报告 — RTOSshoubiao（STM32F103 OLED 手表）

> 体检日期：2026-08-30 · 方式：只读静态抽查（未编译、未上硬件）· 判定人：验收官角色
> 判定：**不通过 —— 3 个 P0 全是 FreeRTOS 经典错误集合，但目前"能跑"属于侥幸**

## P0（致命/高危）

| 问题 | 证据 | 影响 |
|---|---|---|
| **EXTI 回调不快进快出**：ISR 内 `for(i<5000)` 忙等消抖，且读非 volatile 的 end_flag/seclect_end | freertos.c:225,237,249,261、ShowMenu.c:53 | 全系统延迟抖动；编译器优化后按键可能"失灵" |
| **任务栈过小**：ShowTimeTask/ShowDHT11Task 仅 128 字(512B)，栈上放整个 u8g2_t + key_data | freertos.c 任务创建、ShowTimeTask.c:61、ShowDHT11.c:31；heap 仅 8KB 装 9 任务（FreeRTOSConfig.h:63） | 栈溢出踩内存，现象随机难查 |
| **共享数据无保护**：g_xQueueMenu 被 6 个任务重复 `xQueueCreate` 覆盖；秒变量跨任务裸读写 | ShowMenu.c:87、ShowDHT11.c:28、ShowClock.c:61 | 句柄泄漏+竞态；重复建 buzzer 软件定时器泄漏 RAM |

## P1（严重）

| 问题 | 证据 | 影响 |
|---|---|---|
| u8g2 全任务共用 static 1KB 缓冲无互斥；SendBuffer 全量阻塞发送 | u8g2_d_memory.c:11、u8x8_byte.c:605 | 偶发花屏/撕裂 |
| 刷新节拍失衡：表盘 250ms 刷一次，数据 1s 才更新 | ShowTimeTask.c:93 | 白耗电 |
| 逻辑 bug：hour_unit>5 才进位（应 23）；temp%20；max 未初始化即用 | ShowTimeTask.c:118、ShowDHT11.c:55,44,61 | 表显错误 |
| DHT11 忙等占 CPU | driver_timer.c:45 | 低实时 |

## 一致性抽查

- 时钟（freertos.c:162、ShowTimeTask.c:111-121）**一致**；温湿度（freertos.c:171、driver_dht11.c:256）**一致**
- 电子木鱼：ShowWoodenFish.c 存在但创建被注释（freertos.c:172）→ **不可达**

## 工程化

构建说明 2/5（零说明）、接线 3/5（仅 .docx）、测试 1/5、版本 2/5（单次归档 commit、产物入库）。

## 升级路线

1. **先瘦身再谈升级**：删 MDK-ARM/{Objects,Listings}、根目录 .map/.lst（约 73M）、shoubiao/ 嵌套副本 124M 移出、复刻/ 33M 移出、Drivers 只留用到的 F1 HAL——355M 可降到 <50M。
2. ISR 只 `xQueueSendFromISR`，消抖移任务侧；所有 `buzzer_init/xQueueCreate` 收敛为一次性初始化。
3. 任务栈 128→512 字起步，heap 8KB→16KB；u8g2 加互斥或改页缓冲（_1 模式 150B）+ RTC 走时。
4. 接线 .docx 转 docs/02_接线.md 入库；README 补构建烧录三节。
